const INITIAL_STATE = {
  users: [],
  superUser: '',
};

const billReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state
  }
};

export default billReducer;
